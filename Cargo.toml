[package]
name = "checks"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
async-trait = "0.1.77"
bitflags = "2.4.1"
tracing = "0.1.40"

[dev-dependencies]
tokio = { version = "1.35.1", features = ["time", "rt", "macros", "tracing"] }
